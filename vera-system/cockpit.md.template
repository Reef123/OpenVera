# Cockpit

*Derived view — regenerated by doc-sync every session. Never hand-edit; never treat as source of truth. Truth lives in state.md / ROADMAP.md / per-project build-state.md — on any disagreement, those files win and this gets fixed to match.*

**Updated:** YYYY-MM-DD (fresh install — nothing synced yet)

## Momentum — last 5 done

| When | Project | What shipped |
|------|---------|--------------|
| — | — | (nothing yet — this fills in as doc-sync runs after each session) |

## Next — one action per active thread

| Project | Next action | Owner |
|---------|-------------|-------|
| — | (nothing yet — run `/start-vague` or `/build new` to start a project, then doc-sync will populate this) | — |

## Top projects

| Project | Stage | Pulse |
|---------|-------|-------|
| — | (nothing yet — projects appear here once one exists under `vera-projects/projects/`) | — |

## Blocked on you (max 3)

*(nothing yet — this fills in only when work is genuinely stalled on a decision, access, or manual action only you can do)*
